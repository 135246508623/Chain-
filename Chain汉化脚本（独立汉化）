local Translations = {
   --[""] = "",
    ["Main Tab</>"] = "主选项",
    ["Remove Adonis anticheat"] = "防踢",
    ["discord server incase u missed notification!discord.gg/UG7bHSQUVA"] = "加入DC群",
    ["Copy discord invite link"] = "点我复制",
    ["Destroy UI"] = "关闭UI",
    ["Chapter 1 script by Neonicf"] = "Chain章节一启动器",
    ["Print Chainsaw is funny chain"] = "是一条有趣的链子",
    ["Random color picker idk"] = "颜色选择",
    ["Combat"] = "战斗",
    ["Set speed to 12-default speed"] = "速度默认12",
    ["Speed by Bloxy"] = "确认修改速度",
    ["Bypass beartrap placement"] = "放置",
    ["ThirdPerson"] = "第三人称",
    ["CHAIN Hitbox expander beta-slightly updated may glitch chain"] = "Chain的碰撞箱可能会导致chain出现故障",
    ["Inf Stamina"] = "无限跑步耐力",
    ["Inf Combat Stamina"] = "无限战斗耐力",
    ["Tomahawk AttackSpeed"] = "战斧攻击速度",
    ["pls slide"] = "滑动改变速度",
    ["Clash Strength"] = "碰撞强度",
    ["God Mode"] = "上帝模式",
    ["WalkSpeed-glitchy kinda"] = "这个暂时不翻译，可以自己试试",
    ["12 Speed"] = "12速度",
    ["Remove Mask on head-third person"] = "第三人称取消面罩",
    ["Misc"] = "设置",
    ["Detect Devs"] = "检测",
    ["Detect Bloodmoon"] = "检测血月",
    ["Detect Chain s Choke"] = "检测冲刺",
    ["Detect Chain s Groundslam"] = "检测炸地",
    ["These only unlock blueprints btw"] = "顺便说一句，可以解锁蓝图",
    ["Unlock all Blueprints"] = "解锁全部蓝图",
    ["Unlock Combatknife"] = "解锁战斗刀",
    ["Unlock Deagle"] = "解锁沙漠之鹰",
    ["Unlock DoubleBarrel"] = "解锁散弹枪",
    ["Unlock Machete"] = "解锁大砍刀",
    ["Unlock M1911"] = "解锁M1911",
    ["Unlock SpellBook(may be glitched)"] = "解锁手册可能会出故障",
    ["Unlock Artifact Quest(might fix spellbook blueprint)"] = "解锁封印书可能会出故障",
    ["AutoFarm"] = "自动收集",
    ["Auto collect Scrap"] = "自动收集废料",
    ["Auto collect Artifact"] = "自动收集封印书碎片",
    ["Watch and Power Gui"] = "手表和电机查看",
    ["Radio Gui Shop Gui-Only works during day"] = "查看电机和商店",
    ["Radio Gui"] = "查看电机",
    ["Guis deconstructor"] = "废料解析台",
    ["GUI workbench"] = "查看工作台",
    ["See Chat"] = "查看聊天",
    ["Shiftlock thing by bloxy idk"] = "锁定",
    ["These are mostly people who will ban/report you so be careful!"] = "小心他们可能会去举报你，甚至封禁你！",
    ["Detect Special Thanks people"] = "检测特别感谢的人",
    ["Detect Contributers"] = "检测贡献者",
    ["Detect Testers"] = "检测测试人员",
    ["Detect Mods/Admins(include server mods)"] = "检测管理员/检测服务器",
    ["Detect Managers Dotect Deve"] = "检测重要开发者",
    ["Detect Managers"] = "检测经理",
    ["Dotect Deve"] = "检测",
}

local function translateText(text)
    if not text or type(text) ~= "string" then return text end
    
    if Translations[text] then
        return Translations[text]
    end
    
    for en, cn in pairs(Translations) do
        if text:find(en) then
            return text:gsub(en, cn)
        end
    end
    
    return text
end

local function setupTranslationEngine()
    local success, err = pcall(function()
        local oldIndex = getrawmetatable(game).__newindex
        setreadonly(getrawmetatable(game), false)
        
        getrawmetatable(game).__newindex = newcclosure(function(t, k, v)
            if (t:IsA("TextLabel") or t:IsA("TextButton") or t:IsA("TextBox")) and k == "Text" then
                v = translateText(tostring(v))
            end
            return oldIndex(t, k, v)
        end)
        
        setreadonly(getrawmetatable(game), true)
    end)
    
    if not success then
        warn("元表劫持失败:", err)
       
        local translated = {}
        local function scanAndTranslate()
            for _, gui in ipairs(game:GetService("CoreGui"):GetDescendants()) do
                if (gui:IsA("TextLabel") or gui:IsA("TextButton") or gui:IsA("TextBox")) and not translated[gui] then
                    pcall(function()
                        local text = gui.Text
                        if text and text ~= "" then
                            local translatedText = translateText(text)
                            if translatedText ~= text then
                                gui.Text = translatedText
                                translated[gui] = true
                            end
                        end
                    end)
                end
            end
            
            local player = game:GetService("Players").LocalPlayer
            if player and player:FindFirstChild("PlayerGui") then
                for _, gui in ipairs(player.PlayerGui:GetDescendants()) do
                    if (gui:IsA("TextLabel") or gui:IsA("TextButton") or gui:IsA("TextBox")) and not translated[gui] then
                        pcall(function()
                            local text = gui.Text
                            if text and text ~= "" then
                                local translatedText = translateText(text)
                                if translatedText ~= text then
                                    gui.Text = translatedText
                                    translated[gui] = true
                                end
                            end
                        end)
                    end
                end
            end
        end
        
        local function setupDescendantListener(parent)
            parent.DescendantAdded:Connect(function(descendant)
                if descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox") then
                    task.wait(0.1)
                    pcall(function()
                        local text = descendant.Text
                        if text and text ~= "" then
                            local translatedText = translateText(text)
                            if translatedText ~= text then
                                descendant.Text = translatedText
                            end
                        end
                    end)
                end
            end)
        end
        
        pcall(setupDescendantListener, game:GetService("CoreGui"))
        local player = game:GetService("Players").LocalPlayer
        if player and player:FindFirstChild("PlayerGui") then
            pcall(setupDescendantListener, player.PlayerGui)
        end
        
        while true do
            scanAndTranslate()
            task.wait(3)
        end
    end
end

task.wait(2)

setupTranslationEngine()

local success, err = pcall(function()
--这下面填加载外部脚本
loadstring(game:HttpGet('https://pastebin.com/raw/LESum4rq'))()


end)

if not success then
    warn("加载失败:", err)
end
